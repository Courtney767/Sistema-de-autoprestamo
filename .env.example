# URL absoluta del backend (deje vacío en desarrollo para usar el proxy de Vite → http://127.0.0.1:4010)
# VITE_API_BASE_URL=http://localhost:4010

# Simular respuestas sin backend (también puede activarse con el botón "Mock" en dev)
# VITE_USE_MOCK_API=true
